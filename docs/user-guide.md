# ユーザーガイド

1. `samples/sample-input.json` をコピーして対象データを入力する。
2. `npm start` を実行する。
3. `dist/run/report.md` と `dist/run/report.html` を確認する。
4. `public/index.html` をローカルプレビューとして開き、closed alpha の主要導線を確認する。
